
// components/WelcomeView.tsx
import { useEffect, useState } from 'react';
import { motion } from 'framer-motion';
import {
  ArrowRight,
  Bike,
  ChefHat,
  Clock,
  Crown,
  IndianRupee,
  PhoneCall,
  Star,
  Store,
  TrendingUp,
  Users,
  Zap,
} from 'lucide-react';
import { Button } from '@/components/livekit/button';

interface WelcomeViewProps {
  startButtonText?: string;
  onStartCall: () => void;
}

export const WelcomeView = ({
  startButtonText = 'Connect with Aarav Instantly',
  onStartCall,
}: WelcomeViewProps) => {
  const [mounted, setMounted] = useState(false);

  useEffect(() => setMounted(true), []);

  return (
    <div className="relative min-h-screen overflow-hidden bg-black">
      {/* Premium Gradient Background */}
      <div className="absolute inset-0">
        <div className="absolute inset-0 bg-gradient-to-br from-red-950 via-black to-orange-950" />
        <div className="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent" />

        {/* Animated Luxury Orbs */}
        <motion.div
          animate={{
            x: [0, 100, 0],
            y: [0, -100, 0],
          }}
          transition={{ duration: 20, repeat: Infinity, ease: 'linear' }}
          className="absolute top-20 left-20 h-96 w-96 rounded-full bg-gradient-to-r from-red-600/40 to-orange-600/20 blur-3xl"
        />
        <motion.div
          animate={{
            x: [0, -150, 0],
            y: [0, 100, 0],
          }}
          transition={{ duration: 25, repeat: Infinity, ease: 'linear' }}
          className="absolute right-10 bottom-10 h-80 w-80 rounded-full bg-gradient-to-l from-amber-600/30 to-red-700/20 blur-3xl"
        />
      </div>

      {/* Floating Premium Elements */}
      <div className="pointer-events-none absolute inset-0">
        {[
          { Icon: Bike, delay: 0, size: 80, color: 'text-red-500' },
          { Icon: ChefHat, delay: 2, size: 70, color: 'text-amber-400' },
          { Icon: Crown, delay: 4, size: 90, color: 'text-yellow-500' },
          { Icon: IndianRupee, delay: 1.5, size: 75, color: 'text-emerald-400' },
        ].map(({ Icon, delay, size, color }, i) => (
          <motion.div
            key={i}
            initial={{ y: 100, opacity: 0 }}
            animate={{ y: [-20, 20, -20], opacity: [0.3, 0.6, 0.3] }}
            transition={{ duration: 12 + i * 2, repeat: Infinity, delay }}
            className="absolute"
            style={{
              top: `${20 + i * 15}%`,
              left: i % 2 === 0 ? `${15 + i * 10}%` : 'auto',
              right: i % 2 === 1 ? `${10 + i * 8}%` : 'auto',
            }}
          >
            <Icon
              className={`h-${size / 4} w-${size / 4} ${color} drop-shadow-2xl`}
              strokeWidth={1.5}
            />
          </motion.div>
        ))}
      </div>

      <div className="relative z-10 flex min-h-screen flex-col items-center justify-center px-6">
        <motion.div
          initial={{ opacity: 0, y: 40 }}
          animate={{ opacity: mounted ? 1 : 0, y: mounted ? 0 : 40 }}
          transition={{ duration: 1.2, ease: 'easeOut' }}
          className="max-w-7xl text-center"
        >
          {/* Royal Logo */}
          <motion.div
            whileHover={{ scale: 1.05 }}
            className="group relative mx-auto mb-12 inline-block cursor-pointer"
          >
            <div className="absolute -inset-8 animate-pulse rounded-full bg-gradient-to-r from-red-600/50 to-amber-600/30 blur-3xl" />
            <div className="relative flex h-40 w-40 items-center justify-center rounded-full bg-gradient-to-br from-red-600 via-red-500 to-orange-600 p-1 shadow-2xl ring-8 ring-red-500/40">
              <div className="flex h-full w-full items-center justify-center rounded-full bg-black/60 backdrop-blur-xl">
                <div className="rounded-full bg-gradient-to-br from-amber-400 to-red-600 p-8 shadow-2xl ring-4 ring-white/20">
                  <Store className="h-24 w-24 text-white" strokeWidth={3} />
                </div>
              </div>
            </div>
            <Crown className="absolute -top-6 right-8 h-16 w-16 text-yellow-500 drop-shadow-2xl" />
          </motion.div>

          {/* Headline */}
          <h1 className="mb-6 bg-gradient-to-r from-amber-400 via-red-400 to-amber-300 bg-clip-text text-6xl font-black tracking-tighter text-transparent md:text-8xl">
            Partner with India’s #1
          </h1>
          <h2 className="mb-8 text-4xl font-bold text-white md:text-6xl">Food Delivery Platform</h2>

          <p className="mx-auto mb-12 max-w-4xl text-xl leading-relaxed text-gray-300 md:text-2xl">
            Join <span className="font-bold text-amber-400">350,000+ premium restaurants</span>{' '}
            earning <span className="font-bold text-emerald-400">₹10L–₹5Cr extra per year</span>{' '}
            with Zomato
          </p>

          {/* Elite Benefit Cards (Glassmorphism + Tilt) */}
          <div className="mb-16 grid grid-cols-1 gap-8 md:grid-cols-3">
            {[
              {
                icon: TrendingUp,
                title: '10× Order Growth',
                desc: 'Average partner sees 8-12× more orders in first 3 months',
                color: 'from-emerald-500 to-teal-600',
              },
              {
                icon: IndianRupee,
                title: 'Highest Payouts',
                desc: 'Get paid weekly • Zero hidden charges • Best rates in India',
                color: 'from-amber-500 to-orange-600',
              },
              {
                icon: Star,
                title: 'VIP Support',
                desc: 'Dedicated manager • Priority listing • Gold-tier visibility',
                color: 'from-yellow-500 to-amber-600',
              },
            ].map((card, i) => (
              <motion.div
                key={i}
                initial={{ opacity: 0, y: 60 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: i * 0.2 }}
                whileHover={{ y: -12, rotateX: 8, rotateY: 8 }}
                className="group relative"
              >
                <div
                  className="absolute -inset-1 rounded-3xl bg-gradient-to-r opacity-70 blur-xl transition-all group-hover:opacity-100"
                  style={{
                    backgroundImage: `linear-gradient(to right, ${card.color.split(' ')[1]}, ${card.color.split(' ')[3]})`,
                  }}
                />
                <div className="relative rounded-3xl border border-white/10 bg-white/5 p-8 backdrop-blur-2xl">
                  <card.icon
                    className={`mb-4 h-16 w-16 bg-gradient-to-br ${card.color} bg-clip-text text-transparent`}
                  />
                  <h3 className="mb-3 text-2xl font-bold text-white">{card.title}</h3>
                  <p className="text-gray-300">{card.desc}</p>
                </div>
              </motion.div>
            ))}
          </div>

          {/* Ultra Premium CTA */}
          <motion.div
            initial={{ scale: 0.9 }}
            animate={{ scale: 1 }}
            transition={{ duration: 0.8, delay: 0.6 }}
          >
            <Button
              onClick={onStartCall}
              className="group relative overflow-hidden rounded-full bg-gradient-to-r from-amber-500 via-red-600 to-orange-600 px-20 py-10 text-4xl font-black tracking-wider text-white shadow-2xl transition-all duration-500 hover:scale-105 hover:shadow-amber-500/50"
            >
              <span className="relative z-10 flex items-center gap-6">
                <PhoneCall className="h-14 w-14 transition-all group-hover:rotate-12 group-hover:text-yellow-300" />
                {startButtonText}
                <ArrowRight className="h-12 w-12 transition-all group-hover:translate-x-6 group-hover:text-yellow-300" />
              </span>
              <motion.div
                className="absolute inset-0 bg-gradient-to-r from-yellow-400 to-amber-600"
                initial={{ x: '-100%' }}
                whileHover={{ x: '100%' }}
                transition={{ duration: 0.6 }}
              />
            </Button>
          </motion.div>

          <motion.p
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 1 }}
            className="mt-10 text-xl text-gray-400"
          >
            Instant call with{' '}
            <span className="bg-gradient-to-r from-amber-400 to-red-400 bg-clip-text font-bold text-transparent">
              Aarav
            </span>{' '}
            • Your Personal Zomato Growth Manager
            <br />
            <span className="text-sm">Limited slots • High-value partners only</span>
          </motion.p>
        </motion.div>
      </div>
    </div>
  );
};
