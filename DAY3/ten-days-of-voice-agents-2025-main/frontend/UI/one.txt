// components/app/welcome-view.tsx
import { Button } from '@/components/livekit/button';
import { Mic, Sparkles, Heart, Activity, Brain, Sun, Cloud, Zap, Flower2 } from 'lucide-react';
import { motion } from 'framer-motion';
import React from 'react';

const floatingIcons = [
  { Icon: Brain, color: "text-orange-500", delay: 0 },
  { Icon: Flower2, color: "text-pink-500", delay: 0.5 },
  { Icon: Sun, color: "text-yellow-400", delay: 1 },
  { Icon: Activity, color: "text-purple-500", delay: 1.5 },
  { Icon: Sparkles, color: "text-emerald-400", delay: 2 },
  { Icon: Cloud, color: "text-blue-400", delay: 2.5 },
];

export const WelcomeView = React.forwardRef<HTMLDivElement, { startButtonText: string; onStartCall: () => void }>(
  ({ startButtonText, onStartCall }, ref) => {
    return (
      <div
        ref={ref}
        className="relative min-h-screen overflow-hidden bg-gradient-to-br from-orange-50 via-pink-50 to-purple-50"
      >
        {/* Animated Gradient Orbs Background */}
        <div className="absolute inset-0">
          <motion.div
            animate={{
              x: [-100, 100, -100],
              y: [-100, 100, -100],
            }}
            transition={{ repeat: Infinity, duration: 20, ease: "linear" }}
            className="absolute -top-40 -left-40 w-96 h-96 rounded-full bg-gradient-to-r from-orange-400/30 to-pink-400/20 blur-3xl"
          />
          <motion.div
            animate={{
              x: [100, -100, 100],
              y: [100, -100, 100],
            }}
            transition={{ repeat: Infinity, duration: 25, ease: "linear" }}
            className="absolute -bottom-40 -right-40 w-96 h-96 rounded-full bg-gradient-to-l from-purple-400/30 to-emerald-400/20 blur-3xl"
          />
        </div>

        {/* Floating Wellness Icons Around the Edges */}
        {floatingIcons.map(({ Icon, color, delay }, i) => (
          <motion.div
            key={i}
            initial={{ opacity: 0, scale: 0 }}
            animate={{ opacity: 0.6, scale: 1 }}
            transition={{ delay: delay + 1, duration: 1 }}
            className={`absolute ${i % 2 === 0 ? '-left-8 top-32' : '-right-8 top-48'} hidden lg:block`}
          >
            <motion.div
              animate={{ y: [0, -30, 0], rotate: [0, 360, 0] }}
              transition={{ repeat: Infinity, duration: 15 + i * 2, delay }}
            >
              <Icon className={`w-20 h-20 ${color} drop-shadow-2xl`} />
            </motion.div>
          </motion.div>
        ))}

        <section className="relative z-10 flex min-h-screen flex-col items-center justify-center px-6 text-center">
          {/* Hero Illustration */}
          <motion.div
            initial={{ scale: 0.8, opacity: 0 }}
            animate={{ scale: 1, opacity: 1 }}
            transition={{ duration: 1.2, ease: "easeOut" }}
            className="relative mb-12"
          >
            <div className="relative">
              {/* Pulsing Glow Ring */}
              <motion.div
                animate={{ scale: [1, 1.3, 1] }}
                transition={{ repeat: Infinity, duration: 4 }}
                className="absolute inset-0 rounded-full bg-gradient-to-r from-orange-500/40 via-pink-500/40 to-purple-500/40 blur-3xl"
              />

              {/* Main Heart + Activity Pulse */}
              <div className="relative">
                <motion.div
                  animate={{ scale: [1, 1.15, 1] }}
                  transition={{ repeat: Infinity, duration: 3 }}
                >
                  <Heart className="w-36 h-36 md:w-48 md:h-48 text-white drop-shadow-3xl" fill="currentColor" />
                </motion.div>
                <motion.div
                  animate={{ scale: [1, 1.4, 1] }}
                  transition={{ repeat: Infinity, duration: 2.5, ease: "easeInOut" }}
                  className="absolute inset-0 rounded-full bg-gradient-to-r from-orange-500 to-pink-600 opacity-60 blur-xl"
                />
                <Activity className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-24 h-24 md:w-32 md:h-32 text-white" />
              </div>
            </div>
          </motion.div>

          {/* Headline */}
          <motion.h1
            initial={{ y: 40, opacity: 0 }}
            animate={{ y: 0, opacity: 1 }}
            transition={{ delay: 0.6, duration: 1 }}
            className="bg-gradient-to-r from-orange-600 via-pink-600 to-purple-700 bg-clip-text text-5xl md:text-7xl lg:text-8xl font-black tracking-tight text-transparent"
          >
            Your Daily<br />Wellness Coach
          </motion.h1>

          <motion.p
            initial={{ y: 30, opacity: 0 }}
            animate={{ y: 0, opacity: 1 }}
            transition={{ delay: 0.9, duration: 1 }}
            className="mt-8 text-xl md:text-2xl lg:text-3xl text-gray-700 font-medium max-w-4xl"
          >
            One gentle voice. One daily check-in. Real progress — every single day.
          </motion.p>

          {/* CTA Button – The Star of the Show */}
          <motion.div
            initial={{ scale: 0.8, opacity: 0 }}
            animate={{ scale: 1, opacity: 1 }}
            transition={{ delay: 1.4, duration: 0.8, type: "spring", stiffness: 200 }}
            className="mt-16"
          >
            <Button
              onClick={onStartCall}
              className="group relative overflow-hidden rounded-full bg-gradient-to-r from-orange-500 via-pink-500 to-purple-600 px-24 py-10 text-2xl md:text-3xl font-bold text-white shadow-3xl transition-all duration-500 hover:shadow-orange-500/50 hover:scale-110"
            >
              <span className="relative z-10 flex items-center gap-5">
                <Mic className="w-10 h-10 md:w-12 md:h-12" />
                {startButtonText}
              </span>

              {/* Shine Effect */}
              <motion.div
                className="absolute inset-0 bg-white/30"
                initial={{ x: "-100%" }}
                whileHover={{ x: "100%" }}
                transition={{ duration: 0.8 }}
              />
              {/* Particle Burst on Hover */}
              <motion.div
                className="absolute inset-0"
                whileHover={{
                  background: [
                    "radial-gradient(circle at 20% 50%, rgba(255,255,255,0.4) 0%, transparent 50%)",
                    "radial-gradient(circle at 80% 50%, rgba(255,255,255,0.4) 0%, transparent 50%)",
                  ],
                }}
                transition={{ duration: 0.6 }}
              />
            </Button>
          </motion.div>

          {/* Feature Pills */}
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 1.8 }}
            className="mt-20 flex flex-wrap justify-center gap-8 md:gap-12"
          >
            {["Daily Check-ins", "Mood Memory", "Tiny Habits", "Zero Judgment"].map((text, i) => (
              <motion.div
                key={text}
                initial={{ y: 30, opacity: 0 }}
                animate={{ y: 0, opacity: 1 }}
                transition={{ delay: 2 + i * 0.15 }}
                className="flex items-center gap-3 rounded-full bg-white/80 backdrop-blur-md px-8 py-4 shadow-xl"
              >
                <div className="h-3 w-3 rounded-full bg-gradient-to-r from-orange-500 to-pink-500" />
                <span className="font-semibold text-gray-800">{text}</span>
              </motion.div>
            ))}
          </motion.div>

          {/* Made in India Badge */}
          <motion.div
            initial={{ scale: 0 }}
            animate={{ scale: 1 }}
            transition={{ delay: 2.6, type: "spring" }}
            className="mt-20 rounded-full bg-white/90 px-10 py-5 shadow-2xl backdrop-blur-xl"
          >
            <p className="text-lg font-bold text-gray-800">
              Proudly Built in India
            </p>
          </motion.div>
        </section>

        {/* Bottom Help Card */}
        <motion.div
          initial={{ y: 50, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ delay: 2.8 }}
          className="fixed bottom-8 left-1/2 -translate-x-1/2"
        >
          <div className="rounded-3xl bg-white/95 backdrop-blur-2xl border border-white/50 px-10 py-6 shadow-2xl">
            <p className="text-lg font-medium text-gray-700">
              First time here?{' '}
              <a
                href="https://docs.livekit.io/agents/start/voice-ai/"
                target="_blank"
                rel="noopener noreferrer"
                className="bg-gradient-to-r from-orange-600 to-pink-600 bg-clip-text text-transparent font-bold underline"
              >
                Tap here for 30-second setup
              </a>
            </p>
          </div>
        </motion.div>
      </div>
    );
  }
);

WelcomeView.displayName = "WelcomeView";