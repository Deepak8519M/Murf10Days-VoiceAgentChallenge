// components/app/welcome-view.tsx
import React from 'react';
import { motion } from 'framer-motion';
import { Activity, Calendar, HeartPulse, PhoneCall, ShieldCheck, Sparkles } from 'lucide-react';
import { Button } from '@/components/livekit/button';

const apollo = {
  primary: '#1A504C',
  accent: '#00D1C1',
  bg: '#0B1218',
  card: 'rgba(17, 26, 32, 0.7)',
  text: '#E2E8F0',
};

// Floating Brand Card Component
const BrandCard = ({ delay, position, brand, color, icon: Icon }: any) => (
  <motion.div
    initial={{ opacity: 0, scale: 0.8, ...position.initial }}
    animate={{ opacity: 1, scale: 1, ...position.animate }}
    transition={{ delay, duration: 1.2, type: 'spring', stiffness: 100 }}
    className="pointer-events-none absolute z-10"
  >
    <div
      className="shadow-3xl rounded-3xl border border-white/20 p-6 backdrop-blur-2xl"
      style={{
        background: 'rgba(255, 255, 255, 0.08)',
        boxShadow: '0 20px 40px rgba(0,0,0,0.6), inset 0 4px 20px rgba(255,255,255,0.1)',
      }}
    >
      <div className="flex flex-col items-center gap-3">
        <div className="rounded-full p-4" style={{ backgroundColor: color }}>
          {Icon && <Icon className="h-10 w-10 text-white" />}
        </div>
        <p className="text-2xl font-black tracking-wider text-white">{brand}</p>
        <p className="text-xs font-light text-gray-400">
          {brand === 'Murf AI' ? 'Powered By' : 'Design Inspired'}
        </p>
      </div>
    </div>
  </motion.div>
);

export const WelcomeView = React.forwardRef<
  HTMLDivElement,
  { startButtonText: string; onStartCall: () => void }
>(({ startButtonText, onStartCall }, ref) => {
  return (
    <div
      ref={ref}
      className="relative min-h-screen w-screen overflow-hidden"
      style={{ backgroundColor: apollo.bg }}
    >
      {/* Top gradient */}
      <div
        className="h-1 w-full"
        style={{ background: `linear-gradient(90deg, ${apollo.primary}, ${apollo.accent})` }}
      />

      {/* Floating Brand Cards — All identical premium style */}
      <BrandCard
        delay={1.4}
        position={{
          initial: { x: -200, y: -100 },
          animate: { x: '8vw', y: '20vh' },
        }}
        brand="Apollo 24|7"
        color="#1A504C"
        icon={HeartPulse}
      />

      <BrandCard
        delay={1.7}
        position={{
          initial: { x: 300, y: -150 },
          animate: { x: '80vw', y: '68vh' },
        }}
        brand="PharmEasy"
        color="#E91E63"
      />

      <BrandCard
        delay={1.9}
        position={{
          initial: { x: 250, y: 300 },
          animate: { x: '15vw', y: '65vh' },
        }}
        brand="Tata 1mg"
        color="#FF6D00"
      />

      <BrandCard
        delay={2.1}
        position={{
          initial: { y: -200 },
          animate: { x: '80vw', y: '20vh' },
        }}
        brand="Murf AI"
        color="#8B5CF6"
        icon={Sparkles}
      />

      {/* Subtle glow orbs */}
      <motion.div
        animate={{ y: ['-15vh', '15vh', '-15vh'] }}
        transition={{ repeat: Infinity, duration: 30, ease: 'easeInOut' }}
        className="pointer-events-none fixed top-[10vh] left-[5vw] h-[50vw] w-[50vw] rounded-full opacity-25 blur-3xl"
        style={{ background: `radial-gradient(circle, ${apollo.accent}40, transparent 70%)` }}
      />

      {/* Main Content */}
      <div className="relative z-20 flex min-h-screen flex-col items-center justify-center px-6 py-16">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          className="text-center"
        >
          <h1
            className="bg-gradient-to-br from-[#1A504C] to-[#00D1C1] bg-clip-text font-black tracking-tighter text-transparent"
            style={{ fontSize: 'clamp(3rem, 11vw, 10rem)', lineHeight: '1' }}
          >
            Daily Health
            <br />
            Check‑in
          </h1>

          <motion.p
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.8 }}
            className="mx-auto mt-8 max-w-3xl leading-relaxed font-light text-gray-300"
            style={{ fontSize: 'clamp(1rem, 3.5vw, 2rem)' }}
          >
            Your personal voice companion — trusted like Apollo, easy like PharmEasy, reliable like
            Tata 1mg, and powered by Murf AI.
          </motion.p>
        </motion.div>

        <motion.div
          initial={{ scale: 0.95 }}
          animate={{ scale: 1 }}
          transition={{ delay: 1.2 }}
          className="mt-16"
        >
          <Button
            onClick={onStartCall}
            className="group shadow-3xl relative overflow-hidden rounded-full transition-all duration-500 hover:shadow-[0_0_90px_rgba(0,209,193,0.8)]"
            style={{
              background: `linear-gradient(135deg, ${apollo.primary}, ${apollo.accent})`,
              padding: 'clamp(1.5rem, 5vw, 4rem) clamp(5rem, 14vw, 12rem)',
              fontSize: 'clamp(1.3rem, 5vw, 2.8rem)',
            }}
          >
            <span className="relative z-10 flex items-center gap-6">
              <PhoneCall
                style={{ width: 'clamp(3rem, 8vw, 5rem)', height: 'clamp(3rem, 8vw, 5rem)' }}
              />
              {startButtonText}
            </span>
            <motion.div
              className="absolute inset-0 bg-white/30"
              animate={{ x: ['-150%', '150%'] }}
              transition={{ duration: 3.5, repeat: Infinity }}
            />
          </Button>
        </motion.div>

        {/* Feature Icons */}
        <motion.div
          initial={{ opacity: 0, y: 50 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 1.8 }}
          className="mt-24 grid w-full max-w-5xl grid-cols-2 gap-12 md:grid-cols-4"
        >
          {[
            { icon: HeartPulse, label: 'Mood & Energy' },
            { icon: Calendar, label: 'Daily Check‑ins' },
            { icon: ShieldCheck, label: '100% Private' },
            { icon: Activity, label: 'Real Progress' },
          ].map((item, i) => (
            <motion.div key={i} whileHover={{ y: -12 }} className="text-center">
              <div className="mx-auto mb-5 flex h-36 w-36 items-center justify-center rounded-3xl bg-white/10 shadow-2xl backdrop-blur-xl">
                <item.icon className="h-20 w-20" style={{ color: apollo.accent }} />
              </div>
              <p
                className="font-medium text-gray-200"
                style={{ fontSize: 'clamp(1rem, 3vw, 1.5rem)' }}
              >
                {item.label}
              </p>
            </motion.div>
          ))}
        </motion.div>
      </div>

      {/* Floating Help */}
      <motion.div
        initial={{ y: 50 }}
        animate={{ y: 0 }}
        transition={{ delay: 2.5 }}
        className="fixed bottom-8 left-1/2 z-50 -translate-x-1/2"
      >
        <div
          className="shadow-3xl rounded-full bg-white/10 px-12 py-5 backdrop-blur-xl"
          style={{ border: `3px solid ${apollo.accent}` }}
        >
          <p className="text-center font-medium text-white">
            First time?{' '}
            <a
              href="https://docs.livekit.io/agents/start/voice-ai/"
              target="_blank"
              rel="noopener noreferrer"
              className="font-bold underline"
              style={{ color: apollo.accent }}
            >
              Quick setup
            </a>
          </p>
        </div>
      </motion.div>
    </div>
  );
});

WelcomeView.displayName = 'WelcomeView';
